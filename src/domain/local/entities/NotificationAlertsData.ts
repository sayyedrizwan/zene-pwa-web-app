export class NotificationAlertsData {
  img: string
  title: string
  desc: string

  constructor(img: string, title: string, desc: string,) {
    this.img = img
    this.title = title
    this.desc = desc
  }
}